import { P } from '../../style/typography';
import styled from 'styled-components';

const PageDescriptionContainer = styled.div`
  margin: 0 0 0 17em;
`;

const Description = styled(P)`
  width: 19em;
  line-height: 1.8em;
`;

export { PageDescriptionContainer, Description };
